<h2 class="survey-title">Make A'Vey</h2>
<form class="form-basic" id='createSurvey'>

  <div class="form-row">
    <label>
      <span>Survey Title</span>
      <textarea name="survey[title]">
        {{#survey.survey}}
          {{this.title}}
        {{/survey.survey}}
      </textarea>
    </label>
  </div>

  <div class="form-row">
    <label>
      <span>Description Text </span>
      <textarea name="survey[description]">
        {{#survey.survey}}
          {{this.description}}
        {{/survey.survey}}
      </textarea>
    </label>
  </div>

  <div class="form-row">
    <label>
      <span>Survey Question</span>
      <textarea name="survey[questions][text]">
        {{#survey.survey}}
          {{#this.questions}}
            {{this.text}}
          {{/this.questions}}
        {{/survey.survey}}
      </textarea>
    </label>
  </div>

  <div class="form-row">
    <label>
      <span>Survey Type</span>
      <select name="survey[type]">
        <option id="multiple-choice">Multiple Choice</option>
        <option id="scale">Scale</option>
        <option id="text">Text</option>
      </select>
    </label>
  </div>

  <div class="form-row">
    <label>
      <span>Choice one</span>
      <textarea name="survey[questions][choice][questone]">
        {{#survey.survey}}
          {{#this.questions}}
            {{this.text}}
          {{/this.questions}}
        {{/survey.survey}}
      </textarea>
    </label>
  </div>

  <div class="form-row">
    <label>
      <span>Choice two</span>
      <textarea name="survey[questions][choice][questtwo]">
        {{#survey.survey}}
          {{#this.questions}}
            {{this.text}}
          {{/this.questions}}
        {{/survey.survey}}
      </textarea>
    </label>
  </div>

  <div class="form-row">
    <label>
      <span>Choice three</span>
      <textarea name="survey[questions][choice][questthree]">
        {{#survey.survey}}
          {{#this.questions}}
            {{this.text}}
          {{/this.questions}}
        {{/survey.survey}}
      </textarea>
    </label>
  </div>

  <div class="form-row">
    <label>
      <span>Choice four</span>
      <textarea name="survey[questions][choice][questfour]">
        {{#survey.survey}}
          {{#this.questions}}
            {{this.text}}
          {{/this.questions}}
        {{/survey.survey}}
      </textarea>
    </label>
  </div>

  <div class="form-row">
    <label>
      <span>Choice five</span>
      <textarea name="survey[questions][choice][questfive]">
        {{#survey.survey}}
          {{#this.questions}}
            {{this.text}}
          {{/this.questions}}
        {{/survey.survey}}
      </textarea>
    </label>
  </div>


<p> There will be a text field for survey takers to fill in.</p>

  <div class="form-row">
    <button type="submit" class="btn btn-info create-survey-submit">Take It A'vey!</button>
  </div>

</form>
